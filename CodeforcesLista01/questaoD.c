#include <stdio.h>

int h(int a, int b){
  int s;
  s = a + b;
  return 0;
}

int mult() {
  int total = a*60+b*60;
  return 0;
}

int main() {
  int a, b;
  printf("%d\n", mult(a, b));
 );
scanf("%d%*c%d%", &hora, &min;
  return 0;
}